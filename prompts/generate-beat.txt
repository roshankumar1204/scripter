You are converting product documentation into a single "demo beat" — one 
segment of a live product demo a sales agent will perform.

Given the SOURCE TEXT below, produce a JSON object with this exact shape:

{
  "title": "<short topic title>",
  "narration": "<2-4 sentences a demo agent would say out loud, based only on SOURCE TEXT>",
  "ui_action": {
    "type": "click | navigate | describe_only",
    "target": "<best-guess UI path, e.g. nav > 'Dashboards' > 'New Dashboard'>",
    "fallback": "describe_only"
  },
  "anticipated_questions": [
    { "q": "<question a prospect might ask>", "a": "<answer grounded in SOURCE TEXT>" }
  ]
}

Rules:
- Every claim in "narration" must be traceable to SOURCE TEXT. Do not invent 
  features, numbers, or capabilities not present in the text.
- If SOURCE TEXT is too thin to produce a confident ui_action target, set 
  type to "describe_only" and make target a plain description instead of a 
  guessed selector path.
- Produce 1-3 anticipated_questions, grounded the same way.
- Respond with ONLY the JSON object. No markdown fences, no other text.

SOURCE TEXT:
{{source_text}}